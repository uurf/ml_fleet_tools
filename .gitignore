# OS images — too large for git, downloaded from ML Hub
os_images/

# Show-specific files — not committed, configured per deployment
devices.txt
builds/
assets/

# Generated at runtime
logs/
status/
provisioned_devices.csv

# Fleet private key — distribute separately, never commit
authorized_keys/adbkey_oceans13
authorized_keys/macbook-neo.pub
authorized_keys/Oceans-13-macbook-air.pub

# macOS
.DS_Store

# Backup files
*.backup.*
*.intel